version: "3"

tasks:
  generate:bindings:
    desc: "Generate TypeScript bindings from Rust types via ts-rs"
    sources:
      - crates/redpen-core/src/**/*.rs
      - src-tauri/src/**/*.rs
      - crates/redpen-core/Cargo.toml
      - src-tauri/Cargo.toml
    generates:
      - src/lib/bindings/*.ts
    cmds:
      - cargo test --workspace -- export_bindings

  check:bindings:
    desc: "Check that generated TypeScript bindings are up-to-date"
    cmds:
      - task: generate:bindings
      - git diff --exit-code src/lib/bindings/ || (echo "Bindings are stale. Run 'task generate:bindings' and commit." && exit 1)

  dev:
    desc: "Run Tauri in development mode"
    cmds:
      - bun run tauri dev

  dev:up:
    desc: "Install all dependencies (frontend + Rust)"
    cmds:
      - bun install
      - cargo check --workspace
      - task: generate:bindings

  dev:clean:
    desc: "Remove installed desktop app and CLI"
    cmds:
      - |
        APP="/Applications/Red Pen.app"
        if [ -d "$APP" ]; then
          rm -rf "$APP"
          echo "Removed $APP"
        else
          echo "No installed app found at $APP"
        fi
      - |
        CLI=$(which redpen 2>/dev/null || true)
        if [ -n "$CLI" ]; then
          cargo uninstall redpen-cli 2>/dev/null && echo "Uninstalled redpen CLI" || echo "CLI not installed via cargo"
        else
          echo "No redpen CLI found in PATH"
        fi

  dev:install:
    desc: "Build and install the desktop app and CLI locally"
    cmds:
      - bun run tauri build
      - |
        # Install desktop app
        APP="/Applications/Red Pen.app"
        if [ -d "$APP" ]; then
          echo "Removing existing $APP"
          rm -rf "$APP"
        fi
        cp -R target/release/bundle/macos/Red\ Pen.app /Applications/
        echo "Installed Red Pen.app to /Applications"
      - |
        # Install CLI
        cargo install --path crates/redpen-cli
        echo "Installed redpen-cli to $(which redpen)"

  sign:
    desc: "Sign and notarize the already-built app bundle"
    dotenv: [".env.signing"]
    cmds:
      - |
        codesign --force --deep --sign "$APPLE_SIGNING_IDENTITY" \
          "target/release/bundle/macos/Red Pen.app"
        echo "Signed Red Pen.app"
      - |
        xcrun notarytool submit "target/release/bundle/dmg/Red Pen_$(cat src-tauri/tauri.conf.json | python3 -c 'import json,sys;print(json.load(sys.stdin)["version"])')_aarch64.dmg" \
          --apple-id "$APPLE_ID" \
          --password "$APPLE_PASSWORD" \
          --team-id "$APPLE_TEAM_ID" \
          --wait
        echo "Notarized"
      - |
        xcrun stapler staple "target/release/bundle/dmg/Red Pen_$(cat src-tauri/tauri.conf.json | python3 -c 'import json,sys;print(json.load(sys.stdin)["version"])')_aarch64.dmg"
        echo "Stapled"

  build:signed:
    desc: "Build and sign the app (sources .env.signing)"
    dotenv: [".env.signing"]
    cmds:
      - bun run tauri build
      - task: sign

  release:upload:
    desc: "Upload the signed DMG and CLI binary to the GitHub release"
    cmds:
      - |
        VERSION=$(python3 -c 'import json; print(json.load(open("src-tauri/tauri.conf.json"))["version"])')
        TAG="v${VERSION}"
        if ! gh release view "$TAG" &>/dev/null; then
          gh release create "$TAG" --title "$TAG" --notes ""
          echo "Created release $TAG"
        fi

        # Upload DMG
        DMG="target/release/bundle/dmg/Red Pen_${VERSION}_aarch64.dmg"
        gh release upload "$TAG" "$DMG" --clobber
        echo "Uploaded $DMG to $TAG"

        # Build and upload CLI binary tarball
        cargo build --release -p redpen-cli
        TARBALL="redpen-${VERSION}-aarch64-apple-darwin.tar.gz"
        tar -czf "$TARBALL" -C target/release redpen
        gh release upload "$TAG" "$TARBALL" --clobber
        rm -f "$TARBALL"
        echo "Uploaded $TARBALL to $TAG"

  release:brew:
    desc: "Update homebrew-tap with latest release"
    cmds:
      - ./scripts/update-homebrew-tap.sh

  release:
    desc: "Bump version, update all manifests, commit, tag, and push. Usage: task release BUMP=patch|minor|major"
    requires:
      vars: [BUMP]
    preconditions:
      - sh: '[ "{{.BUMP}}" = "patch" ] || [ "{{.BUMP}}" = "minor" ] || [ "{{.BUMP}}" = "major" ]'
        msg: "BUMP must be one of: patch, minor, major"
      - sh: "git diff --quiet && git diff --cached --quiet"
        msg: "Working tree must be clean before releasing"
    cmds:
      - |
        # Get current version from latest tag (strip leading v), default to 0.1.0
        TAG=$(git describe --tags --abbrev=0 2>/dev/null || true)
        if [ -z "$TAG" ]; then
          CURRENT="0.1.0"
        else
          CURRENT=$(echo "$TAG" | sed 's/^v//')
        fi
        IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"

        case "{{.BUMP}}" in
          major) MAJOR=$((MAJOR + 1)); MINOR=0; PATCH=0 ;;
          minor) MINOR=$((MINOR + 1)); PATCH=0 ;;
          patch) PATCH=$((PATCH + 1)) ;;
        esac

        NEW="${MAJOR}.${MINOR}.${PATCH}"
        echo "Bumping version: ${CURRENT} -> ${NEW}"

        # Update package.json
        sed -i '' "s/\"version\": \".*\"/\"version\": \"${NEW}\"/" package.json

        # Update tauri.conf.json
        sed -i '' "s/\"version\": \".*\"/\"version\": \"${NEW}\"/" src-tauri/tauri.conf.json

        # Update Cargo.toml files
        for f in src-tauri/Cargo.toml crates/redpen-cli/Cargo.toml crates/redpen-core/Cargo.toml; do
          sed -i '' "s/^version = \".*\"/version = \"${NEW}\"/" "$f"
        done

        # Commit, tag, push
        git add package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml crates/redpen-cli/Cargo.toml crates/redpen-core/Cargo.toml
        git commit -m "release: v${NEW}"
        git tag "v${NEW}"
        git push origin main
        git push origin "v${NEW}"

        echo "Released v${NEW}"
