{
  "description": "Red Pen review gates for pushes and plan documents",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if the Bash command contains 'git push'. If it does, respond with: {\"decision\": \"block\", \"reason\": \"Run /review-code to review changes in Red Pen before pushing.\"}. If it does NOT contain 'git push', respond with: {\"decision\": \"approve\"}. Only block on 'git push' — do not block other git commands.",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash $CLAUDE_PLUGIN_ROOT/hooks/scripts/check-plan-write.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
