[package]
name = "redpen-runtime"
version = "0.1.6"
edition = "2021"

[dependencies]
redpen-core = { path = "../redpen-core" }
thiserror = "2"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
